# v0.5 Release Notes (2026-02-22)

## Overview

v0.5는 v0.4에서 구현된 기능들의 실제 동작을 구현하고 음성 명령어 기능을 추가한 메이저 업데이트입니다. WASM 기반 플러그인 샌드박스 실행, 외부 서비스와의 실제 데이터 연동, 템플릿 버전 관리 및 공유, 그리고 멀티턴 음성 대화가 추가되었습니다.

## What's New

### 🔌 WASM Plugin Runtime (Phase 1)

**WASM Sandbox Execution**
- Wasmtime 기반 샌드박스 실행 환경
- WASI(WebAssembly System Interface) 호스트 구현
- 리소스 제한: 메모리, CPU, 실행 시간 모니터링
- 플러그인 생명주크 관리: 시작, 중지, 재시작

**Plugin Executor**
- `plugins/runtime.rs`: Wasmtime 런타임 래퍼
- `plugins/wasi_host.rs`: 플러그인용 WASI 컨텍스트 제공
- `plugins/monitor.rs`: 리소스 사용량 실시간 모니터링
- `plugins/executor.rs`: WASM 런타임과 통합된 플러그인 실행기

### 🔗 Integration Services (Phase 2)

**Database Integration**
- PostgreSQL 연결 및 쿼리 실행 (`tokio-postgres`)
- MySQL 연결 및 쿼리 실행 (`mysql_async`)
- 연결 풀 관리 및 타임아웃 처리

**Git Operations**
- 저장소 상태 확인 (`git2` 라이브러리)
- commit, push, pull 연동
- 사용자 정보 설정 관리

**Cloud Storage**
- AWS S3 업로드/다운로드/목록 조회
- S3 클라이언트 설정 및 연결 관리

### 📄 Template Sharing (Phase 3)

**Template Import/Export**
- JSON 형식 템플릿 내보내기/가져오기
- 충돌 해결 전략: skip, overwrite, rename, version
- 대량 템플릿 일괄 처리

**Template Versioning**
- `template_versions` 테이블: 템플릿 버전 히스토리
- 버전 생성 및 롤백 기능
- 버전별 노트 저장

**Team Sharing**
- `template_shares` 테이블: 팀 공유 관리
- 권한 설정: read, write, execute
- 공유 취소 기능

### 🎤 Voice Commands (Phase 4)

**Voice Command Parser**
- 자연어 음성 명령어 파싱
- 영어/한국어 패턴 지원
- 정규식 기반 패턴 매칭

**AgentRuntime Integration**
- `execute_voice_command`: 음성 명령어를 Skill/Recipe/Chat으로 라우팅
- 동작 타입: ExecuteSkill, RunRecipe, SendMessage, OpenFeature, Search
- 알 수 없는 명령어는 채팅으로 자동 전환

**Multi-turn Conversation**
- `start_voice_conversation`: 대화 세션 시작
- `continue_voice_conversation`: 대화 계속 (컨텍스트 유지)
- `end_voice_conversation`: 세션 종료 및 기록 저장

**Voice Pattern Storage**
- `voice_patterns` 테이블: 사용자 정의 음성 패턴
- `voice_conversations` 테이블: 대화 기록
- 언어별 패턴 지원

## Database Schema Changes

### Migration v6: Template Versioning and Sharing
```sql
-- template_versions: 버전 히스토리
CREATE TABLE IF NOT EXISTS template_versions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    template_id TEXT NOT NULL,
    version INTEGER NOT NULL,
    content TEXT NOT NULL,
    notes TEXT,
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    UNIQUE(template_id, version)
);

-- template_shares: 팀 공유
CREATE TABLE IF NOT EXISTS template_shares (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    template_id TEXT NOT NULL,
    team_id TEXT NOT NULL,
    permissions TEXT NOT NULL,
    shared_by TEXT NOT NULL,
    shared_at TEXT NOT NULL DEFAULT (datetime('now')),
    UNIQUE(template_id, team_id)
);
```

### Migration v7: Voice Patterns and Conversations
```sql
-- voice_patterns: 사용자 정의 음성 패턴
CREATE TABLE IF NOT EXISTS voice_patterns (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    pattern TEXT NOT NULL,
    action_type TEXT NOT NULL,
    target_id TEXT,
    language TEXT NOT NULL DEFAULT 'en',
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);

-- voice_conversations: 대화 기록
CREATE TABLE IF NOT EXISTS voice_conversations (
    id TEXT PRIMARY KEY,
    session_id TEXT NOT NULL,
    role TEXT NOT NULL,
    content TEXT NOT NULL,
    language TEXT NOT NULL DEFAULT 'en',
    audio_transcribed INTEGER DEFAULT 0,
    created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

## API Changes

### New Tauri Commands

#### Template Import/Export (Phase 3)
- `export_template(id: String) -> Vec<u8>`
- `export_all_templates() -> Vec<u8>`
- `import_template(data: Vec<u8>, resolution: String) -> Template`
- `import_templates(data: Vec<u8>, resolution: String) -> ImportResult`
- `validate_template_data(data: Value) -> bool`

#### Template Versioning (Phase 3)
- `get_template_versions(id: String) -> Vec<TemplateVersion>`
- `create_template_version(id: String, notes: String) -> i64`
- `rollback_template(id: String, version_id: i64) -> ()`

#### Template Sharing (Phase 3)
- `share_template_to_team(id: String, team_id: String, permissions: Value) -> ()`
- `get_team_templates(team_id: String) -> Vec<Template>`
- `revoke_template_access(id: String, team_id: String) -> ()`

#### Voice Commands (Phase 4)
- `execute_voice_command(transcript: String, language: Option<String>) -> VoiceCommandResult`
- `start_voice_conversation(language: String) -> String`
- `continue_voice_conversation(session_id: String, transcript: String) -> VoiceCommandResult`
- `end_voice_conversation(session_id: String) -> ()`

#### Integration (Phase 2)
- `test_database_connection(config: DatabaseConfig) -> bool`
- `get_database_connection_string(name: String) -> String`
- `validate_git_repository(path: String) -> GitStatus`
- `get_git_status(path: String) -> GitStatus`
- `test_cloud_connection(config: CloudConfig) -> bool`
- `list_cloud_objects(config: CloudConfig) -> Vec<CloudObject>`

#### Plugin Execution (Phase 1)
- `plugin_execute(id: String, operation: String, params: Value) -> Value`
- `plugin_get_resource_usage(id: String) -> ResourceUsage`
- `plugin_send_message(id: String, message: Value) -> ()`
- `plugin_stop(id: String) -> ()`
- `plugin_restart(id: String) -> ()`
- `plugin_list_running() -> Vec<String>`

## Frontend Changes

### New Stores
- `stores/collaborationStore.ts`: 템플릿 import/export 및 버전 관리

### New Types
- `types/collaboration.ts`: TemplateVersion, ImportResult, TemplateShareRequest

## Module Structure

```
src-tauri/src/
├── collaboration/
│   ├── template_io.rs      # JSON import/export
│   ├── template_commands.rs # Tauri commands
│   ├── marketplace.rs       # Template marketplace client
│   └── mod.rs
├── plugins/
│   ├── runtime.rs          # WASM runtime wrapper
│   ├── wasi_host.rs        # WASI host implementation
│   ├── monitor.rs          # Resource monitoring
│   ├── executor.rs         # Plugin execution
│   └── mod.rs
├── integration/
│   ├── database/mod.rs     # PostgreSQL/MySQL
│   ├── git/mod.rs          # Git operations
│   ├── cloud/mod.rs        # AWS S3
│   └── mod.rs
├── voice/
│   ├── commands.rs         # Voice command parser
│   └── mod.rs
└── db/
    └── schema.rs           # Migrations v6, v7
```

## Known Limitations

1. **WASM Plugins**: 실행 중인 플러그인 강제 중지가 불안정할 수 있음
2. **Database**: 연결 풀 크기가 고정됨 (기본 10)
3. **Git**: SSH 키 파일만 지원 (비밀키 내용 직접 전송 불가)
4. **S3**: multipart upload 미구현 (대용량 파일 제한)
5. **Voice Conversations**: 대화 기록이 메모리에만 저장됨 (DB 저장 예정)
6. **Voice Patterns**: 사용자 정의 패턴 UI 미구현

## Breaking Changes

없음. 모든 변경은 하위 호환됩니다.

## Migration Guide

v0.4 → v0.5 업그레이드 시:
1. `npm install`로 새로운 의존성 설치
2. `cd src-tauri && cargo build`로 Rust 백엔드 빌드
3. 데이터베이스 마이그레이션 자동 수행 (v6, v7)

## Performance

- WASM 플러그인 실행: ~10-50ms冷启动
- DB 쿼리: 연결 풀 사용으로 재사용 가능
- 음성 명령어 파싱: <1ms

## Security

- 플러그인 샌드박스: WASM 보안 경계 적용
- 리소스 제한: 메모리 512MB, CPU 30초
- API 키: 암호화 저장 (v0.5 보안 모듈)

## Future Work (v0.6)

- AI 에이전트 시스템 개선
- 워크플로우 자동화
- 고급 음성 기능 (음성 합성, 감정 인식)
- 플러그인 마켓플레이스 실제 연동
- 클라우드 동기화

## Acknowledgments

Generated with [Claude Code](https://claude.com/claude-code)
via [Happy](https://happy.engineering)

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
